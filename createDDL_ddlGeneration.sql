CREATE TABLE PERSONNE (ID INTEGER NOT NULL, NOM VARCHAR(255), PRENOM VARCHAR(255), CHIEN_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE CHIEN (ID INTEGER NOT NULL, NOM VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE PERSONNE ADD CONSTRAINT FK_PERSONNE_CHIEN_ID FOREIGN KEY (CHIEN_ID) REFERENCES CHIEN (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
